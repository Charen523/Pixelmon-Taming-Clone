
public enum PixelmonState
{
    Idle,
    Move,
    Attack
}

public enum FieldState
{
    Locked,
    Buyable,
    Empty,
    Seeded,
    Harvest
}

public enum PassiveType
{
    Attack,
    Buff,
    Farming
}